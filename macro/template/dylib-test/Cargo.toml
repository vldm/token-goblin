[workspace]

[package]
name = "generated-crate" #{{package.name}}
version = "0.1.0"
edition = "2024"
# {{package.extra}}

[lib]
crate-type = ["dylib"]

[dependencies]
token-goblin-runtime = { path = "../../runtime" } # "0.1"

# {{dependencies}}
