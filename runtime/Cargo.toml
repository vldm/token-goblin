[package]
name = "token-goblin-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
proc-macro2 = { version = "1.0", default-features = false, features = [
    "span-locations",
] }
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
